#include <iostream>
#include <stdio.h>
#include <math.h>
#include <cstdlib>
#include <algorithm>
#include <list>
#include <vector>
#include <string>
#include <set>

using namespace std;

std::vector <std::string> str;

std::map<char, bool> inset;

int main(){
	char s1[30],s2[30],s3[1010],s[30];
	int i,j,k,n;
	cin >> s1;
	for (i=0;i<n;i++){
		
		inset.second() = true;
	}
	cin >> s2;
	cin >> n;
	for (i=0;i<n;i++){
		cin >> s;

	}
}